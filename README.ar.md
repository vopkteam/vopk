# VOPK — الواجهة الموحدة لإدارة الحزم (سابقًا APKG)

**VOPK** هو الجيل الجديد من **الواجهة الموحدة لإدارة الحزم عبر التوزيعات**، مبني ومدعوم من فريق **GP Team**.  
يوفّر **سطر أوامر واحد** لكل التوزيعات الشهيرة في لينكس — بنفس الأوامر، وسلوك متوقع، وتجربة استخدام أوضح.

> 🆕 **الإصدار 1.0.0 – أكبر تحديث في تاريخ APKG**  
> - تغيير اسم المشروع من **APKG → VOPK**  
> - أول إصدار **مستقر** رسميًا (خارج مرحلة الـ beta)  
> - واجهة استخدام أفضل، وـ UX أوضح، واختصارات أذكى  
> - تحسين وضع الـ dry-run (بدون أسئلة تأكيد بلا معنى)  
> - دعم اختياري لـ **vmpkg** كـ backend عند الحاجة  
> - عشرات الإصلاحات والتحسينات في الأمان والسلوك

سواءً كنت على Debian، Ubuntu، Arch، Fedora، openSUSE، Alpine، Void، Gentoo، أو داخل Container / WSL / VM — **VOPK يعمل ببساطة**.

---

## 🚀 لماذا VOPK؟

مدراء الحزم التقليديون مرتبطون بالتوزيعة نفسها:

- `apt` / `apt-get` لـ Debian / Ubuntu  
- `pacman` لـ Arch  
- `dnf` / `yum` لـ Fedora / RHEL / CentOS  
- `zypper` لـ openSUSE  
- `apk` لـ Alpine  
- `xbps` لـ Void  
- `emerge` لـ Gentoo  

**VOPK يحل مشكلة هذا التشظّي** عن طريق توفير **CLI موحّد ومتناسق** فوق كل هذه المدراء.

أمثلة:

```bash
# بدلًا من:
sudo dnf update
sudo pacman -S neovim
sudo apt remove firefox

# يمكنك استخدام:
sudo vopk update
sudo vopk install neovim
sudo vopk remove firefox
```

نفس الصيغة. نفس طريقة التفكير. على كل التوزيعات المدعومة.

---

## ✨ أبرز ما في الإصدار 1.0.0

- ✅ **اسم جديد وهوية جديدة**: من `apkg` إلى **`vopk`**
- ✅ أول إصدار **مستقر** (1.0.0) — لم يعد أداة تجريبية
- ✅ **CLI موحّد** لكل التوزيعات الشهيرة في لينكس
- ✅ دعم **apt / apt-get, pacman, dnf, yum, zypper, apk, xbps, emerge**
- ✅ **اكتشاف تلقائي للـ backend** المناسب للنظام
- ✅ وضع **dpkg fallback** (يعمل حتى لو apt غير موجود)
- ✅ دعم اختياري لـ **vmpkg** عندما لا يتوفر مدير حزم للنظام
- ✅ **تجربة استخدام سهلة للمبتدئين**: رسائل أوضح وخيارات أكثر أمانًا
- ✅ وضع **dry-run نظيف**:
  - يوضّح ما الذي سيحدث لو نفذت الأمر
  - **بدون أسئلة تأكيد `y/n` أثناء `--dry-run`**
- ✅ اختصارات قصيرة ومريحة للأوامر الشائعة
- ✅ وضع تمرير مباشر للـ backend لأغراض السكربتات (`vopk script-v` / `vopk backend`)
- ✅ تنفيذ Bash محصّن (`set -euo pipefail`)
- ✅ سكربت واحد فقط، بدون تبعيات إضافية (إلى جانب مدير الحزم نفسه)
- ✅ أوامر مساعدة مفيدة: معلومات النظام، تثبيت أدوات التطوير، إصلاح DNS، وغير ذلك

VOPK مصمم ليكون مريحًا للمبتدئين، وفي نفس الوقت قويًا كفاية للمستخدمين المتقدمين والسكربتات.

---

## 📦 مدراء الحزم المدعومون

| الـ Backend           | التوزيعات / ملاحظات                                       |
|-----------------------|-----------------------------------------------------------|
| **apt / apt-get**     | Ubuntu, Debian, Mint, PopOS, Kali, إلخ                    |
| **pacman**            | Arch, Manjaro, EndeavourOS, إلخ                           |
| **dnf**               | Fedora                                                    |
| **yum**               | CentOS, RHEL (قديم)                                       |
| **zypper**            | openSUSE                                                 |
| **apk**               | Alpine Linux                                             |
| **xbps-install**      | Void Linux                                               |
| **emerge**            | Gentoo                                                   |
| **dpkg (fallback)**   | أنظمة Debian التي لا تحتوي على apt/apt-get               |
| **vmpkg (اختياري)**   | إذا كان `vmpkg` مثبتًا ولا يوجد مدير حزم للنظام         |

VOPK يكتشف تلقائيًا الـ backend المناسب حسب بيئتك.

---

## 🔥 بديل مباشر لـ APT (وأكثر)

يمكن لـ VOPK أن يعمل **بوجود apt أو بدونه**:

- إذا كان `apt` / `apt-get` موجودًا → يستخدمه VOPK كـ backend  
- إذا لم يكن `apt` موجودًا على نظام Debian ولكن `dpkg` موجود → يتحول VOPK إلى وضع **dpkg-only**  
- في وضع dpkg-only، يدعم VOPK تثبيت ملفات `.deb` المحلية وبعض العمليات الأساسية

مثال:

```bash
# استخدام عادي (backend هو apt)
sudo vopk install htop

# على نظام Debian مصغّر بدون apt:
sudo vopk install ./custom-package.deb
# vopk سيستخدم dpkg مباشرة للتعامل مع ملف الـ .deb
```

بهذا يصبح VOPK واجهة إدارة حزم يمكن الاعتماد عليها حتى على الأنظمة المصغرة وبيئات الـ containers.

---

## 🤝 التكامل مع vmpkg (Backend اختياري)

في حال وجود **[vmpkg](https://github.com/gpteamofficial/vmpkg)** مثبّت، يمكن لـ VOPK أن يعمل كواجهة مريحة له أيضًا:

- إذا لم يتم العثور على مدير حزم للنظام ولكن `vmpkg` موجود → يستخدم VOPK **vmpkg** كـ backend
- ويمكنك استدعاء vmpkg صراحةً عبر VOPK:

```bash
# تمرير مباشر لأوامر vmpkg عبر vopk
vopk vm install my-package
vopk vmpkg list
```

متغيرات البيئة مثل `VMPKG_ASSUME_YES` و `VMPKG_DRY_RUN` وغيرها يتم تمريرها تلقائيًا عندما يستخدم VOPK الـ vmpkg كـ backend.

> ملاحظة: VOPK لا يستبدل منطق vmpkg نفسه؛ بل يوفّر فقط نقطة دخول ألطف، وإحساس موحّد عندما تتنقل بين بيئات مختلفة.  

---

## 🏗 التثبيت
### سكربت التثبيت (موصى به)

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/gpteamofficial/vopk/main/src/installscript.sh)
```

### طريقة بديلة (في حال حظر process substitution)

```bash
curl -fsSL https://raw.githubusercontent.com/gpteamofficial/vopk/main/src/installscript.sh | sudo bash
```

---

## 🛠 الصيانة (تحديث / إصلاح / إعادة تثبيت / حذف)

استخدم سكربت الصيانة:

```bash
curl -fsSL https://raw.githubusercontent.com/gpteamofficial/vopk/main/src/updatescript.sh | sudo bash
```

من خلال هذا السكربت يمكنك:

- تحديث VOPK  
- إصلاح التثبيت الحالي  
- إعادة تثبيت VOPK  
- حذف VOPK  
- حذف VOPK مع أخذ نسخة احتياطية

---

## 📚 أساسيات الاستخدام

الأوامر الأساسية:

```bash
# تحديث قاعدة بيانات الحزم
vopk update

# ترقية الحزم المثبتة
vopk upgrade

# ترقية النظام بالكامل
vopk full-upgrade

# تثبيت / إزالة / إزالة مع الإعدادات
vopk install <package> [...]
vopk remove  <package> [...]
vopk purge   <package> [...]

# إزالة الحزم اليتيمة / غير المستخدمة
vopk autoremove

# البحث والمعلومات
vopk search <pattern>
vopk list
vopk show <package>

# تنظيف كاش الحزم
vopk clean
```

### الاختصارات القصيرة

لتسهيل الحياة (خصوصًا للمبتدئين)، يقدّم VOPK مجموعة من الاختصارات القصيرة:

```bash
# اختصارات
vopk i   <pkg>      # install
vopk rm  <pkg>      # remove
vopk up             # update + upgrade
vopk fu             # full-upgrade
vopk ls             # list
vopk s   <pattern>  # search
vopk si  <pkg>      # show info
```

كل هذه الاختصارات اختيارية — الأوامر الكاملة تعمل دائمًا.

### وضع backend مباشر (للمستخدمين المتقدمين)

إذا كنت تحتاج للتعامل مع مدير الحزم الفعلي مباشرةً، بدون طبقة الحماية الخاصة بـ VOPK:

```bash
# تشغيل backend الحقيقي كما هو
vopk script-v <backend-args>
vopk backend  <backend-args>

# أمثلة (تختلف حسب التوزيعة)
vopk script-v install -y htop          # apt/apt-get/dnf/…
vopk script-v -Syu                     # pacman
```

هذا مفيد للسكربتات والمستخدمين المتقدمين الذين يريدون نقطة دخول موحدة (`vopk`) مع التحكم الكامل في backend.

---

## 📂 إدارة المستودعات (Repositories)

```bash
# عرض المستودعات المعرّفة
vopk repos-list

# إضافة مستودع (التنفيذ يعتمد على backend)
vopk add-repo <args...>

# إزالة / تعطيل إدخالات مستودع عن طريق pattern
vopk remove-repo <pattern>
```

السلوك يختلف من backend لآخر (apt, zypper, yum/dnf, apk, إلخ)، ومصمّم ليكون **محافظًا وآمنًا** قدر الإمكان.

---

## 🧠 أدوات النظام والتطوير

يتضمن VOPK أيضًا مجموعة أوامر مساعدة:

```bash
# النظام والتشخيص
vopk sys-info      # معلومات عامة عن النظام
vopk kernel        # إصدار النواة
vopk disk          # استخدام الأقراص
vopk mem           # استخدام الذاكرة
vopk top           # تشغيل htop/top
vopk ps            # أكثر العمليات استهلاكًا للموارد
vopk ip            # معلومات الشبكة

# أدوات التطوير (compilers, git, curl, إلخ)
vopk install-dev-kit

# محاولة إصلاح مشاكل DNS الشائعة
vopk fix-dns

# فحص البيئة والـ backend
vopk doctor
```

أمر `vopk doctor` مفيد جدًا عندما تحاول استكشاف أخطاء غريبة على الأنظمة المصغرة، أو داخل الـ containers، أو WSL.

---

## 🌍 الخيارات العامة ومتغيرات البيئة

الـ flags العامة:

- `-y, --yes, --assume-yes` → افتراض الإجابة "نعم" على أسئلة التأكيد  
- `-n, --dry-run` → عرض ما سيحدث بدون تنفيذ أي تغييرات، وبدون أي أسئلة تأكيد  
- `--no-color` → تعطيل الألوان في المخرجات  
- `--debug` → مخرجات إضافية للتصحيح (debug)  
- `-q, --quiet` → إخفاء رسائل المعلومات والاكتفاء بالتحذيرات/الأخطاء

متغيرات البيئة (الخاصة بـ VOPK):

- `VOPK_SUDO=""` → تعطيل sudo/doas (تشغيل الأوامر كما هي)  
- `VOPK_SUDO="doas"` → استخدام `doas` بدل `sudo`  
- `VOPK_ASSUME_YES=1` → افتراض "نعم" في أسئلة التأكيد (مفيد في السكربتات)  
- `VOPK_DRY_RUN=1` → تفعيل وضع dry-run عالميًا  
- `VOPK_NO_COLOR=1` → تعطيل الألوان  
- `VOPK_DEBUG=1` → تفعيل مخرجات debug  
- `VOPK_QUIET=1` → إخفاء رسائل المعلومات

متغيرات بيئة قديمة (من حقبة APKG — ما زالت مدعومة):

- `APKG_SUDO`, `APKG_ASSUME_YES`, `APKG_DRY_RUN`, `APKG_NO_COLOR`, `APKG_DEBUG`, `APKG_QUIET`  
  → ما زالت محترمة، ويتم ربطها داخليًا بالمتغيرات الجديدة في VOPK.

أمثلة:

```bash
VOPK_ASSUME_YES=1 vopk upgrade
VOPK_SUDO="doas"  vopk install neovim

# أسلوب قديم (ما زال يعمل، للتوافق مع الإصدارات السابقة)
APKG_ASSUME_YES=1 vopk upgrade
```

---

## 🧱 أهداف التصميم

- **موحّد**: واجهة واحدة وطريقة تفكير واحدة بغض النظر عن التوزيعة  
- **آمن**: إعدادات افتراضية محافظة، مع تأكيد صريح قبل أي إجراء حساس  
- **سريع**: طبقة خفيفة فوق مدراء الحزم الأصلية، بدون تعقيد زائد  
- **واضح**: لا يخفي أخطاء الـ backend، بل يعرضها بوضوح  
- **مناسب للمبتدئين**: اختصارات سهلة، رسائل مفهومة، وأوامر مساعدة مفيدة

---

## 💚 الشكر والتقدير

VOPK يتم تطويره وصيانته بواسطة **GP Team**.  
بدأ المشروع تحت اسم **APKG**، وهذا الإصدار 1.0.0 هو أول إصدار مستقر رسميًا تحت اسم **VOPK**.

المشروع وُجد لتبسيط إدارة الحزم في لينكس عبر كل البيئات.

---

## ⭐ دعم المشروع

إذا أعجبك VOPK:

- ⭐ قم بعمل Star للمستودع  
- 🐛 افتح تقارير أخطاء / طلبات ميزات  
- 📣 شاركه مع مجتمعك / أصدقائك / زملائك  
- 🔁 ساعد في توفيره كتطبيق جاهز لتوزيعتك أو صورك (images) المخصصة

استمتع بالبكجدات :)🐧 