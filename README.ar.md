# APKG — مدير الحزم الموحد من GP Team (نسخة عربية)

**APKG** هو مدير حزم موحّد يعمل على توزيعات لينكس مختلفة، مطوَّر ومُدار بواسطة **فريق GP Team**.  
يوفّر لك **سطر أوامر واحد** لإدارة الحزم على معظم توزيعات لينكس، مع نفس الأوامر ونفس الخيارات وسلوك متوقّع في كل مكان.

سواء كنت على Debian أو Ubuntu أو Arch أو Fedora أو openSUSE أو Alpine أو Void أو Gentoo أو حتى داخل WSL / حاوية / آلة افتراضية — **APKG يشتغل بس**.

---

## 🚀 ليه APKG؟

في العادة كل توزيعة ليها مدير حزم خاص بيها:

- `apt` لـ Debian / Ubuntu  
- `pacman` لـ Arch  
- `dnf / yum` لـ Fedora / CentOS / RHEL  
- `zypper` لـ openSUSE  
- `apk` لـ Alpine  
- `xbps` لـ Void  
- `emerge` لـ Gentoo  

ده بيخلي حياتك صعبة لو بتتنقل بين توزيعات مختلفة، أو لو شغال على أكتر من سيرفر بمنصات مختلفة.

**APKG بيحل المشكلة دي** إنه يوفّر لك **CLI موحّد** فوق كل مديري الحزم دول.

### مثال

```bash
# بدل ما تحتاج تفتكر كل واحد لوحده:
sudo dnf update
sudo pacman -S neovim
sudo apt remove firefox

# تقدر تستخدم:
sudo apkg update
sudo apkg install neovim
sudo apkg remove firefox
```

نفس الأوامر. نفس المنطق. بس باسم واحد: `apkg`.

---

## ✨ المميزات

- ✔ **واجهه موحّدة** على معظم توزيعات لينكس  
- ✔ يدعم: **apt / apt-get, pacman, dnf, yum, zypper, apk, xbps, emerge**  
- ✔ **اكتشاف تلقائي** لمدير الحزم المناسب حسب التوزيعة  
- ✔ **وضع dpkg fallback** (يشتغل حتى لو apt مش موجود على أنظمة Debian)  
- ✔ تصميم حذر وآمن (`set -euo pipefail` + منطق محافظ في التنفيذ)  
- ✔ سكربت Bash واحد، خفيف، من غير تبعيات إضافية  
- ✔ إعدادات آمنة مع تفضيلات خاصة بكل توزيعة  
- ✔ يشتغل كويس على:
  - السيرفرات  
  - الديسكتوب  
  - WSL  
  - الحاويات (containers)  
  - الآلات الافتراضية (VMs)  
  - الأنظمة المضمّنة (embedded)

- ✔ أدوات مساعدة مدمجة لمعلومات النظام (نواة، رام، مساحة، IP، العمليات، إلخ)

---

## 📦 مديري الحزم المدعومين

| الـ Backend          | التوزيعات المدعومة                               |
|----------------------|--------------------------------------------------|
| **apt / apt-get**    | Ubuntu, Debian, Mint, PopOS, Kali, ...          |
| **pacman**           | Arch, Manjaro, EndeavourOS, ...                  |
| **dnf**              | Fedora                                           |
| **yum**              | CentOS, RHEL (قديم)                              |
| **zypper**           | openSUSE                                         |
| **apk**              | Alpine Linux                                     |
| **xbps**             | Void Linux                                       |
| **emerge**           | Gentoo                                           |
| **dpkg (fallback)**  | أنظمة Debian اللي مافيهاش apt / apt-get         |

**APKG** بيختار الـ backend المناسب أوتوماتيكيًا حسب النظام.

---

## 🔥 بديل مباشر لـ APT (Drop-in replacement)

**APKG** يقدر يشتغل **مع apt أو من غيره**:

- لو `apt` أو `apt-get` موجودين → APKG بيستخدمهم كـ backend  
- لو مش موجودين بس النظام Debian وفيه `dpkg` → APKG يدخل في **وضع dpkg-only**  
- في وضع dpkg-only تقدر تثبّت ملفات `.deb` المحلية وتعمل شوية عمليات أساسية على الحزم

### مثال

```bash
# استخدام عادي (backend = apt)
sudo apkg install htop

# على نظام Debian صغير مفيهوش apt:
sudo apkg install ./custom-package.deb
# apkg هيستخدم dpkg مباشرة لتثبيت ملف الـ .deb
```

ده بيخلي APKG **مدير حزم قائم بذاته** يقدر يكمّل شغله حتى على أنظمة Debian/Ubuntu minimal.

---

## 🏗 التثبيت

### سكربت التثبيت (الطريقة الموصى بها)

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/gpteamofficial/apkg/main/installscript.sh)
```

### طريقة بديلة (لو الـ shell مش بيدعم process substitution)

```bash
curl -fsSL https://raw.githubusercontent.com/gpteamofficial/apkg/main/installscript.sh | sudo bash
```

---

## 🛠 الصيانة (تحديث / إصلاح / إعادة تثبيت / حذف)

فيه سكربت مخصص لصيانة APKG:

```bash
curl -fsSL https://raw.githubusercontent.com/gpteamofficial/apkg/main/updatescript.sh | sudo bash
```

ومن خلاله تقدر:

- تحديث APKG  
- إصلاح التثبيت  
- إعادة التثبيت  
- حذف APKG  
- حذف APKG مع النسخة الاحتياطية

---

## 📚 الأوامر الأساسية

```bash
# تحديث قواعد بيانات الحزم
apkg update

# ترقية الحزم
apkg upgrade

# ترقية كاملة للنظام (full upgrade)
apkg full-upgrade

# تثبيت / إزالة / إزالة مع الإعدادات
apkg install <package> [...]
apkg remove <package> [...]
apkg purge  <package> [...]

# إزالة الحزم اليتيمة / غير المستخدمة
apkg autoremove

# البحث والمعلومات
apkg search <pattern>
apkg list
apkg show <package>

# تنظيف الكاش
apkg clean
```

---

## 📂 إدارة المستودعات (Repositories)

```bash
# عرض المستودعات الحالية
apkg repos-list

# إضافة مستودع (التنفيذ يختلف حسب الـ backend)
apkg add-repo <args...>

# إزالة / تعطيل أسطر مستودعات حسب pattern
apkg remove-repo <pattern>
```

سلوك الأوامر دي يختلف حسب مدير الحزم (apt, zypper, dnf/yum, apk, إلخ)،  
وكل حاجة متصممة بشكل محافظ عشان **مايبهدلش النظام**.

---

## 🧠 أوامر النظام وأدوات الـ Dev

APKG بيشمل كمان شوية أوامر مساعدة:

```bash
# معلومات النظام والتشخيص
apkg sys-info      # ملخّص عن النظام
apkg kernel        # إصدار النواة
apkg disk          # استخدام الأقراص
apkg mem           # استخدام الذاكرة
apkg top           # تشغيل htop/top
apkg ps            # أكثر العمليات استهلاكًا
apkg ip            # معلومات الشبكة

# تثبيت أدوات التطوير (compilers, git, curl, إلخ)
apkg install-dev-kit

# محاولة إصلاح مشاكل الـ DNS الشائعة
apkg fix-dns
```

---

## 🌍 الخيارات العامة والمتغيرات البيئية

**الـ Global flags:**

- `-y, --yes` → اعتبار الموافقة "نعم" تلقائيًا في الـ prompts

**متغيرات البيئة (Environment variables):**

- `APKG_SUDO=""` → تعطيل sudo/doas (تشغيل الأوامر كما هي)  
- `APKG_SUDO="doas"` → إجبار APKG يستخدم `doas` بدل `sudo`  
- `APKG_ASSUME_YES=1` → اعتبار كل الأسئلة = نعم (مفيد في السكربتات / التشغيل غير التفاعلي)

### أمثلة

```bash
APKG_ASSUME_YES=1 apkg upgrade
APKG_SUDO="doas" apkg install neovim
```

---

## 💚 الشكر وحقوق التطوير

**APKG** تم تطويره ومازال يُدار بواسطة **GP Team**.  
مشروع هدفه يبسط إدارة الحزم على لينكس، خصوصًا لو شغال على أنظمة كتير مختلفة في نفس الوقت.

---

## ⭐ دعم المشروع

لو عجبك APKG:

- ⭐ اعمل Star للريبو على GitHub  
- 🐛 افتح Issues للمشاكل أو الاقتراحات  
- 📣 انشره بين أصحابك والـ community اللي انت فيها  

